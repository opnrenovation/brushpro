[build]
builder = "nixpacks"
buildCommand = "npm install && rm -rf .next/cache && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[variables]
PORT = "3000"
NODE_ENV = "production"
